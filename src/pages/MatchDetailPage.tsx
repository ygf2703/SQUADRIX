import { useEffect, useState } from 'react'; import { ArrowRight, Edit3, Plus, Trash2 } from 'lucide-react'; import { Link, useNavigate, useParams } from 'react-router-dom'; import { ConfirmDialog } from '../components/ConfirmDialog'; import { Completion, KpiCard, LoadingSkeleton, PageHeader, PrimaryButton, SecondaryButton, StatusBadge } from '../components/ui'; import { PermissionGuard } from '../components/PermissionGuard'; import { Tabs } from '../components/Tabs'; import { matchesService } from '../services/matchesService'; import type { Match } from '../types/domain';
const tabs = [{ id: 'overview', label: 'סקירה' }, { id: 'squad', label: 'סגל והרכב' }, { id: 'events', label: 'אירועים' }, { id: 'statistics', label: 'סטטיסטיקות' }, { id: 'notes', label: 'הערות' }];
export function MatchDetailPage() { const { id } = useParams(); const navigate = useNavigate(); const [match, setMatch] = useState<Match | null | undefined>(undefined); const [active, setActive] = useState('overview'); const [confirm, setConfirm] = useState(false); const [deleting, setDeleting] = useState(false); useEffect(() => { void matchesService.list().then((items) => setMatch(items.find((item) => item.id === id) ?? null)).catch(() => setMatch(null)); }, [id]); if (match === undefined) return <LoadingSkeleton lines={5} />; if (!match) return <section className="empty-state"><h2>המשחק לא נמצא</h2><Link to="/matches">חזרה למשחקים</Link></section>; const completed = match.match_status === 'completed'; const remove = async () => { if (!id) return; setDeleting(true); try { await matchesService.remove(id); navigate('/matches'); } finally { setDeleting(false); } }; return <><Link className="back-link" to="/matches"><ArrowRight size={16}/>חזרה למשחקים</Link><PageHeader title={`מכבי כפר סבא מול ${match.opponent_name}`} description={`${match.home_or_away === 'home' ? 'בית' : 'חוץ'} · ${match.competition_name} · ${match.match_date ?? 'מועד טרם נקבע'}`} action={<PermissionGuard roles={['admin','professional_staff']}><span className="inline-actions"><PrimaryButton><Edit3 size={17}/>ערוך משחק</PrimaryButton><SecondaryButton onClick={() => setConfirm(true)}><Trash2 size={17}/>מחיקה</SecondaryButton></span></PermissionGuard>} /><section className="match-hero"><div><StatusBadge status={match.match_status}/><strong>{completed ? `${match.team_score}:${match.opponent_score}` : 'תוצאה טרם הוזנה'}</strong><span>{completed ? 'תוצאה סופית' : 'ממתין לנתוני משחק'}</span></div><Completion completed={completed}/></section><div className="kpis match-kpis"><KpiCard label="סגל" value="טרם הוזן"/><KpiCard label="הרכב" value="טרם הוזן"/><KpiCard label="דקות" value="טרם הוזן"/><KpiCard label="שערים" value={completed ? (match.team_score ?? 0) : 'טרם הוזן'}/></div><Tabs items={tabs} active={active} onChange={setActive}/><section className="data-card detail-content">{active === 'overview' && <MatchOverview match={match}/>} {active === 'squad' && <SquadLayout/>} {active === 'events' && <Events/>} {active === 'statistics' && <p className="muted">הסטטיסטיקות יופיעו לאחר הזנת הסגל ודקות המשחק.</p>} {active === 'notes' && <PermissionGuard roles={['admin','professional_staff']}><p className="muted">אין הערות מקצועיות למשחק זה.</p></PermissionGuard>}</section>{confirm && <ConfirmDialog title={`למחוק את המשחק מול ${match.opponent_name}?`} description="הפעולה תמחק גם את נתוני הסגל והסטטיסטיקות המשויכים למשחק. אין אפשרות לבטל אותה." onCancel={() => setConfirm(false)} onConfirm={() => void remove()} busy={deleting}/>}</>; }
function MatchOverview({ match }: { match: Match }) { return <><h2>פרטי המשחק</h2><dl className="details-list"><div><dt>מסגרת</dt><dd>{match.competition_name}</dd></div><div><dt>בית או חוץ</dt><dd>{match.home_or_away === 'home' ? 'בית' : 'חוץ'}</dd></div><div><dt>תאריך</dt><dd>{match.match_date ?? 'טרם נקבע'}</dd></div><div><dt>סטטוס</dt><dd><StatusBadge status={match.match_status}/></dd></div></dl></> }
function SquadLayout() { return <div className="squad-layout">{['הרכב', 'מחליפים', 'לא שותפו'].map((title) => <section key={title}><h3>{title}</h3><p>לא הוזנו שחקנים לקבוצה זו.</p></section>)}</div> }
function Events() { return <><div className="section-action"><div><h2>אירועי משחק</h2><p>שערים, חילופים, כרטיסים והערות.</p></div><PermissionGuard roles={['admin','professional_staff']}><PrimaryButton><Plus size={17}/>הוסף אירוע</PrimaryButton></PermissionGuard></div><div className="timeline-empty"><span>—</span><p>טרם הוזנו אירועים למשחק.</p></div></> }
