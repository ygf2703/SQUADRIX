import { AlertTriangle, X } from 'lucide-react';
import { PrimaryButton, SecondaryButton } from './ui';
export function ConfirmDialog({ title, description, onCancel, onConfirm, busy, confirmLabel = 'מחיקה', busyLabel = 'מוחק…', destructive = true }: { title: string; description: string; onCancel: () => void; onConfirm: () => void; busy?: boolean; confirmLabel?: string; busyLabel?: string; destructive?: boolean }) { return <div className="dialog-backdrop" role="presentation"><section className="confirm-dialog" role="alertdialog" aria-modal="true" aria-labelledby="confirm-title"><button className="icon-button dialog-close" onClick={onCancel} aria-label="סגירת חלון האישור" title="סגירה"><X size={19}/></button><AlertTriangle className={destructive ? 'danger-icon' : ''} aria-hidden="true"/><h2 id="confirm-title">{title}</h2><p>{description}</p><div><SecondaryButton onClick={onCancel} disabled={busy}>ביטול</SecondaryButton><PrimaryButton className={destructive ? 'destructive' : ''} onClick={onConfirm} disabled={busy}>{busy ? busyLabel : confirmLabel}</PrimaryButton></div></section></div>; }
