import { AlertTriangle, X } from 'lucide-react';
import { PrimaryButton, SecondaryButton } from './ui';
export function ConfirmDialog({ title, description, onCancel, onConfirm, busy }: { title: string; description: string; onCancel: () => void; onConfirm: () => void; busy?: boolean }) { return <div className="dialog-backdrop" role="presentation"><section className="confirm-dialog" role="alertdialog" aria-modal="true" aria-labelledby="confirm-title"><button className="icon-button dialog-close" onClick={onCancel} aria-label="סגירת חלון האישור" title="סגירה"><X size={19}/></button><AlertTriangle className="danger-icon" aria-hidden="true"/><h2 id="confirm-title">{title}</h2><p>{description}</p><div><SecondaryButton onClick={onCancel} disabled={busy}>ביטול</SecondaryButton><PrimaryButton className="destructive" onClick={onConfirm} disabled={busy}>{busy ? 'מוחק…' : 'מחיקה'}</PrimaryButton></div></section></div>; }
